<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.market.dao.ItemDAO">	<!-- DAO 인터페이스가 있는 파일명 -->
<insert id="insertItem">
	insert into item(seller_no, category_no, title)
		values(#{seller_no}, #{category_no}, #{title})
</insert>
<select id="selectItemNo" resultType="int">
	select max(no) from item
</select>
<insert id="insertOption">
	insert into option1(item_no, select1, detail, stock, price)
		values(#{oVo.item_no}, #{oVo.select}, #{oVo.detail}, #{oVo.stock}, #{oVo.price})
</insert>
<insert id="insertFile">
	insert into file(item_no,route)
		 values(#{item_no}, #{file})
</insert>
</mapper>